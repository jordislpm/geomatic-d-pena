export interface routeTypes {
    name: string;
    route: string;
}

export interface HomeServicesProps{
    subtitle: string;
    title: string;
    description: string;
    img: string;
    link: string;
}